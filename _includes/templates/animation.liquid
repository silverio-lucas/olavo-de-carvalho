<template id="_animation-template">
  <div class="animation-main fixed-top">
    {% unless site.deepdive.no_breadcrumbs %}
      {% include body/breadcrumbs.liquid url = "/" %}{% endunless %}
    <div class="content">
      <div class="page"></div>
    </div>
  </div>
</template>